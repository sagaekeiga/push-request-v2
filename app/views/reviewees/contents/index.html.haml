.col-xs-12
  .container
    .row
      .content-show-wrapper
        .page-header
          .pull-right= link_to @repo.text_by_status, reviewees_repo_path(@repo), method: :put, class: "btn #{@repo.btn_by_status}"
          %h3= @repo.full_name
        = render 'dirs', resource: @repo, dir_or_files: @dir_or_files
