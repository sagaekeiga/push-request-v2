%nav.navbar.navbar-default.navbar-fixed-top
  .container-fluid.naved
    .navbar-header
      %button.navbar-toggle.collapsed{ type: 'button',
        aria: { contorls: 'navbar', expanded: 'false' },
        data: { target: '#navbar', toggle: 'collapse' } }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to Settings.meta.site, root_path, class: 'navbar-brand'

    #navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav.pull-right
        %li
          = link_to new_reviewee_registration_path, style: 'color: $color-white;' do
            &nbsp;
            = t('helpers.links.sign_up')
        %li
          = link_to new_reviewee_session_path do
            &nbsp;
            = t('helpers.label.reviewee.log_in')
        %li
          = link_to new_reviewer_session_path do
            &nbsp;
            = t('helpers.label.reviewer.log_in')
